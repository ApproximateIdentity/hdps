* Remove unnecessary returns.
* Use dbSendQuery instead of executeSql since the timers are unused.
* Move readfile to tutils
* Make sure there is a cutoff in both generators and converters
* Check if dependencies still hold
* Remove quotes and row numbers from print statements
